# Exported from Render on 2025-07-23T18:40:57Z
services:
  - type: web
    name: minotes-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app/main.py"
    plan: free
    envVars:
      - key: FLASK_ENV
        value: production
